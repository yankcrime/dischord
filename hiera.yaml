:hierarchy:
  - "nodes/%{::hostname}"
  - "modules/%{calling_module}"
  - "%{environment}"
  - common
:backends:
  - yaml
:yaml:
  :datadir: '/etc/puppet/environments/dischord/hieradata'
:merge_behavior:
  deeper
